apiVersion: v1
kind: Service
metadata:
  name: {{ include "hellok8s-helm.fullname" . }}
  labels:
    {{- include "hellok8s-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hellok8s-helm.selectorLabels" . | nindent 4 }}
